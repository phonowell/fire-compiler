# Fire Compiler

Fast compile everything!

- `.coffee` -> `.ahk`
- `.pug` -> `.html`
- `.styl` -> `.css`
- `.ts` -> `.js`
- `.yaml` -> `.json`

## Install

```shell
pnpm i fire-compile
```

## Usage

```typescript
import compiler from 'fire-compiler'

compiler.compile('1.ts')
```